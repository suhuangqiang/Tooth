<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="take_photo_anim" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/popshow_anim</item>
        <item name="android:windowExitAnimation">@anim/pophidden_anim</item>
    </style>

    <style name="type_anim" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/hosp_type_anim_show</item>
        <item name="android:windowExitAnimation">@anim/hosp_type_anim_hide</item>
    </style>

    <style name="comment_anim" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/pop_comment_show</item>
        <item name="android:windowExitAnimation">@anim/pop_comment_hide</item>
    </style>

    <!--侧滑的listview-->
    <declare-styleable name="SlideMode">
        <attr name="mode">
            <enum name="forbid" value="0"></enum>
            <enum name="right" value="1"></enum>
        </attr>
    </declare-styleable>

</resources>
